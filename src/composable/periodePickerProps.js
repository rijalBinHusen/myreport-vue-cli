export default function (name) {
  if (name === "BaseReportFile") {
    return {
      judul: "Set record to show",
      form: "PeriodePicker",
      store: "BaseReportFile",
      btnValue: "Show",
    };
  }
}
