import { ref } from "vue";

export const selectedPeriode = ref(<number>0);
export const selectedWarehouse = ref('')
export const sheet = ref('')
export const shift = ref(<1|2|3>1)
export const freezePanel = ref(false);